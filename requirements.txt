# Install sphinx with a specific docutils version
# Docutils version is for correct bullet point rendering. Can be rolled forward after the theme is updated to >=0.5.1
# See https://stackoverflow.com/a/68685753/2578171
sphinx==7.1.2; python_version=="3.8"
sphinx==7.2.6; python_version>"3.8"
sphinx-tabs==3.*
sphinx-intl>=2.2
Babel>=2.14
polib>=1.2
PyYAML>=6.0
# Chinese word segmentation for the zh search index (html_search_language='zh');
# without it Sphinx's SearchChinese only indexes Latin words.
jieba>=0.42
# Used in common_conf.py extensions; upstream sphinx_rtd_theme used to bring
# this in transitively but the BZUAV fork's setup.py dropped install_requires.
sphinxcontrib-jquery>=4.1
docutils<0.19
requests>=2.31.0

# lxml for parameter parsing:
lxml

# Install sphinx theme from BZUAV (CyberUAV) fork, pinned to a known-good
# commit. An earlier build of this package shipped matrixhawk.css with
# '#'-style comments — invalid CSS that silently killed the @import chain
# (ardupilot.css -> theme.css), collapsing the whole page layout. Pin and
# bump deliberately; verify layout after every bump.
git+https://github.com/CyberUAV/matrixhawk_sphinx_rtd_theme.git@2d9a0c81acdddcfdf08c64aa667dc45055a31ace

# Sphinx youtube and video plugins:
# This package will require a --force option if you have an older extension installed
# Rerun Sphinxsetup.sh after doing that
git+https://github.com/ArduPilot/sphinxcontrib-youtube.git

# and a parser to use getting posts from Discourse (forum) and insert in FrontEnd
beautifulsoup4

# The pre-commit tool is used to run rapid tools like linters, spellcheckers, and formatters.
pre-commit
